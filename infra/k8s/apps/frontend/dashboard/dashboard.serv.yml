apiVersion: v1
kind: Service
metadata:
    name: dashboard
spec:
    selector:
        app: dashboard
    ports:
        - name: dashboard
          protocol: TCP
          port: 80
          targetPort: 80
