import { CurrentUserMiddleware } from './current-user.middleware';

describe('CurrentUserMiddleware', () => {
	it('should be defined', () => {
		// expect(new CurrentUserMiddleware()).toBeDefined();
	});
});
